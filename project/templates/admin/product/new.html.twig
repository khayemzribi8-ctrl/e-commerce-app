{% extends "base.html.twig" %}

{% block title %}Ajouter un produit - Admin{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-10">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <h1 class="h2">Ajouter un nouveau produit</h1>
                <a href="{{ path('admin_product_index') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Retour
                </a>
            </div>

            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    {{ form_start(form, {'attr': {'class': 'form-product', 'novalidate': 'novalidate', 'enctype': 'multipart/form-data'}}) }}

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.name, 'Nom du produit', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                                {{ form_widget(form.name, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Ex: iPhone 15'}}) }}
                                {{ form_errors(form.name) }}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.category, 'Catégorie', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                                {{ form_widget(form.category, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Ex: Électronique'}}) }}
                                {{ form_errors(form.category) }}
                            </div>
                        </div>
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': '4', 'placeholder': 'Décrivez votre produit...'}}) }}
                        {{ form_errors(form.description) }}
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.price, 'Prix (€)', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                                {{ form_widget(form.price, {'attr': {'class': 'form-control form-control-lg', 'placeholder': '99.99', 'step': '0.01'}}) }}
                                {{ form_errors(form.price) }}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.stock, 'Stock', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                                {{ form_widget(form.stock, {'attr': {'class': 'form-control form-control-lg', 'placeholder': '10', 'min': '0'}}) }}
                                {{ form_errors(form.stock) }}
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        {{ form_label(form.image, 'Image du produit', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        <div class="border-2 border-dashed rounded p-4 text-center" id="dropZone" style="border-style: dashed; border-color: #dee2e6; cursor: pointer; transition: all 0.3s ease;">
                            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="#6c757d" class="bi bi-cloud-arrow-up mb-2" viewBox="0 0 16 16">
                                <path fill-rule="evenodd" d="M7.646 5.146a.5.5 0 0 1 .708 0l2 2a.5.5 0 0 1-.708.708L8.5 6.707V14.5a.5.5 0 0 1-1 0V6.707L6.354 7.854a.5.5 0 1 1-.708-.708l2-2z"/>
                                <path d="M4.406 3.406A3 3 0 0 1 10.5 3c.003 0 .007 0 .01 0H7.5a.5.5 0 0 0 0 1H10.510.01a2 2 0 1 0 3.898.757h.545a3 3 0 0 1-3.854 3.812A2.987 2.987 0 0 1 4.5 5.006a3 3 0 0 1 3.906-2.6.5.5 0 0 0 .153-.846A4 4 0 0 0 4.406 3.406z"/>
                            </svg>
                            <p class="mb-0 text-muted">
                                <strong>Glissez et déposez votre image ici</strong><br>
                                <small>ou cliquez pour sélectionner un fichier</small>
                            </p>
                            <small class="text-muted d-block mt-2">JPG, PNG, GIF, WebP (max 5MB)</small>
                            {{ form_widget(form.image, {'attr': {'class': 'd-none', 'id': 'imageInput'}}) }}
                        </div>
                        <small class="form-text text-muted d-block mt-2" id="fileName"></small>
                        {{ form_errors(form.image) }}
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary btn-lg flex-grow-1">
                            <i class="bi bi-check-circle"></i> Créer le produit
                        </button>
                        <a href="{{ path('admin_product_index') }}" class="btn btn-outline-secondary btn-lg">
                            Annuler
                        </a>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const dropZone = document.getElementById('dropZone');
    const imageInput = document.querySelector('input[type="file"][name*="image"]');
    const fileName = document.getElementById('fileName');

    // Safety checks
    if (!dropZone || !imageInput || !fileName) {
        console.error('Required elements not found:', {
            dropZone: !!dropZone,
            imageInput: !!imageInput,
            fileName: !!fileName
        });
        return;
    }

    // Click to select file
    dropZone.addEventListener('click', () => imageInput.click());

    // Drag and drop
    dropZone.addEventListener('dragover', (e) => {
        e.preventDefault();
        dropZone.style.backgroundColor = '#e7f3ff';
        dropZone.style.borderColor = '#0d6efd';
    });

    dropZone.addEventListener('dragleave', () => {
        dropZone.style.backgroundColor = 'transparent';
        dropZone.style.borderColor = '#dee2e6';
    });

    dropZone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropZone.style.backgroundColor = 'transparent';
        dropZone.style.borderColor = '#dee2e6';

        const files = e.dataTransfer.files;
        if (files.length > 0) {
            imageInput.files = files;
            updateFileName(files[0].name);
        }
    });

    // File input change
    imageInput.addEventListener('change', function() {
        if (this.files.length > 0) {
            updateFileName(this.files[0].name);
        }
    });

    function updateFileName(name) {
        fileName.textContent = '✓ Fichier sélectionné: ' + name;
        fileName.style.color = '#28a745';
    }
});
</script>

<style>
.form-product .form-control:focus,
.form-product .form-control:active {
    border-color: #0d6efd;
    box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
}

#dropZone {
    transition: all 0.3s ease;
}

#dropZone:hover {
    background-color: #f8f9fa;
}
</style>
{% endblock %}
